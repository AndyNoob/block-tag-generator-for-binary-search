pluginManagement {
    repositories {
        gradlePluginPortal()

        // add the ScreamingSandals repository for the Gradle plugin
        maven {
            url = 'https://repo.screamingsandals.org/public'
        }
    }
}
rootProject.name = 'ray_trace_gen'
